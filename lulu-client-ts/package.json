{
  "name": "@trainking/lulu-client",
  "version": "1.0.0",
  "description": "TypeScript client SDK for the lulu game server framework",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["lulu", "game-server", "websocket", "tcp", "client"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
